# FIX: Added GraphQL mutation for creating a room
mutation CreateRoom($input: RoomInput!) {
  createRoom(input: $input) {
    id
    hotelId
    number
    type
    floor
    capacity
    price
    size
    status
    amenities
    features
    condition
    lastCleaned
    nextMaintenance
    images
  }
}
